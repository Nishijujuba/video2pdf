# Issue 128 unique prose fixture impact

Issue 128 changes provider-owned compiler origin completion without changing a
validator gate or its ordering. The focused fixtures therefore rematerialize the
PDF, authenticated compile-source closure, adapter objects and origin edges for
each invocation.

- The positive fixture starts with no usable SyncTeX candidate. Unique whole-run
  prose in list, caption and line-end-hyphenation shapes seeds the existing
  visual-line completion, then the public Rendered Text Reconciliation passes.
- The precedence fixture keeps duplicate path/line matches, unsupported source
  content, display-only content and prose content with a competing display match
  unmapped. Its public adapter input covers multiline and same-line `$$`,
  `\[...\]`, `equation`, and starred `align` syntax. Ordinary `\\[0.5em]`
  line breaks and display-like tokens inside comments remain eligible prose.
  Rejected objects remain explicit `unexpected_addition` edges.
- Existing Issue 125 display resolution, Issue 126 bounded identity work and
  Issue 127 ordered whole-pair fixtures require no rematerialization because
  their source closures and expected first gates are unchanged.

No snapshot, signature, authority artifact, actual Run record or historical
workflow evidence is rewritten by these fixtures.
